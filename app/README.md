# Hanova App

Flutter client for Hanova beauty, clinic, skincare store, appointments, orders,
notifications, and patient communication.

## Run

```powershell
flutter pub get
flutter run
```

The Dart package remains named `app` to preserve existing imports. User-facing
application names are configured as Hanova for Android, iOS, web, Windows,
macOS, and Linux.
